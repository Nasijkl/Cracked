using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum PatternStatus
{
    RandomHead, // 随机行为头部
    RandomEnd, // 随机行为尾部
    RepeatHead, // 循环行为头部
    RepeatEnd, // 循环行为尾部
    Effect // 行为效果
}
